getPreEmployeeApplicationId = select application_id from pre_emp where username = ?

getPreEmployee_1 = select username, application_id, first_name, last_name, email, contact, address,file_location, pre_flag, edit_flag, enable_flag, rmg_done_flag, rmg_admin_done_flag, total_edit_flag from pre_emp where username = ?

getPreEmployeeList_1_1 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id,email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag,pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0 and rmg_done_flag = 0)page_data where rownum >=0
getPreEmployeeList_1_2 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id,email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0 and pe.rmg_done_flag = 1 and pe.rmg_admin_done_flag = 0)page_data where rownum >=0
getPreEmployeeList_1_3 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id,email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0 and pe.rmg_done_flag = 1 and pe.rmg_admin_done_flag = 1)page_data where rownum >=0
getPreEmployeeList_1_4 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id,email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag,pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0)page_data where rownum >=0

getPreEmployeeList_2_1 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id, email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag,pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id  left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0 and rmg_done_flag = 0)page_data where rownum >=?
getPreEmployeeList_2_2 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id, email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag,pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id  left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0 and rmg_done_flag = 0 and pe.first_name like ?)page_data where rownum >=?
getPreEmployeeList_2_3 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id, email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id  left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0 and pe.rmg_done_flag = 1 and pe.rmg_admin_done_flag = 0)page_data where rownum >=?
getPreEmployeeList_2_4 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id, email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id  left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0 and pe.rmg_done_flag = 1 and pe.rmg_admin_done_flag = 0 and pe.first_name like ?)page_data where rownum >=?
getPreEmployeeList_2_5 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id, email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id  left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0 and pe.rmg_done_flag = 1 and pe.rmg_admin_done_flag = 1)page_data where rownum >=?
getPreEmployeeList_2_6 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id, email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id  left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0 and pe.rmg_done_flag = 1 and pe.rmg_admin_done_flag = 1 and pe.first_name like ?)page_data where rownum >=?
getPreEmployeeList_2_7 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id, email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag,pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id  left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0)page_data where rownum >=?
getPreEmployeeList_2_8 = select distinct top 10 bgc_done_flag,application_id, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag, emp_id, email from (select distinct em.email, em.emp_id,hb.bgc_done_flag,pe.application_id, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag,pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id  left join emp em on pe.application_id = em.application_id where pe.edit_flag = 0 and pe.first_name like ?)page_data where rownum >=?

getCount1 = select count(*) from (select distinct bgc_done_flag,application_id, email, first_name,last_name, pre_flag, rmg_done_flag from (select distinct hb.bgc_done_flag,pe.application_id, pe.email, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id where pe.edit_flag = 0 and rmg_done_flag = 0)page_data where rownum >=0) q1
getCount2 = select count(*) from (select distinct bgc_done_flag,application_id, email, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag from (select distinct hb.bgc_done_flag,pe.application_id, pe.email, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag,pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id where pe.edit_flag = 0 and rmg_done_flag = 0 and pe.first_name like ?)page_data where rownum >=0) q1
getCount3 = select count(*) from (select distinct bgc_done_flag,application_id, email, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag from (select distinct hb.bgc_done_flag,pe.application_id, pe.email, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id where pe.edit_flag = 0 and pe.rmg_done_flag = 1 and pe.rmg_admin_done_flag = 0)page_data where rownum >=0) q1
getCount4 = select count(*) from (select distinct bgc_done_flag,application_id, email, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag from (select distinct hb.bgc_done_flag,pe.application_id, pe.email, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id where pe.edit_flag = 0 and pe.rmg_done_flag = 1 and pe.rmg_admin_done_flag = 0 and pe.first_name like ?)page_data where rownum >=0) q1
getCount5 = select count(*) from (select distinct bgc_done_flag,application_id, email, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag from (select distinct hb.bgc_done_flag,pe.application_id, pe.email, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id where pe.edit_flag = 0 and pe.rmg_done_flag = 1 and pe.rmg_admin_done_flag = 1)page_data where rownum >=0) q1
getCount6 = select count(*) from (select distinct bgc_done_flag,application_id, email, first_name,last_name, pre_flag, rmg_done_flag, rmg_admin_done_flag from (select distinct hb.bgc_done_flag,pe.application_id, pe.email, pe.first_name,pe.last_name, pe.pre_flag, pe.rmg_done_flag, pe.rmg_admin_done_flag, ROW_NUMBER() over (order by fd.last_modified_date desc) as rownum from file_data fd left join pre_emp pe on pe.application_id = fd.application_id left join hr_bgc hb on pe.application_id = hb.application_id where pe.edit_flag = 0 and pe.rmg_done_flag = 1 and pe.rmg_admin_done_flag = 1 and pe.first_name like ?)page_data where rownum >=0) q1

getPreEmployee_2 = select username, application_id, first_name, last_name, email, contact, address,file_location, pre_flag, edit_flag, enable_flag, rmg_done_flag, rmg_admin_done_flag, total_edit_flag from pre_emp where application_id = ?

registration_insert1 = insert into pre_emp(username,password,first_name,last_name, email,contact, address, zip, file_location, pre_flag, edit_flag, enable_flag, rmg_done_flag, rmg_admin_done_flag ) values (:username,:password,:firstName,:lastName,:email,null,null,null,:fileLocation,:preFlag,:editFlag, :enableFlag, :rmgDoneFlag,:rmgAdminDoneFlag)
registration_insert2 = insert into emp values (?,?,?,?,?,?,?,?,?,?)
registration_insert3 = insert into file_category(category_name,parent_category_id, application_id, visibility) values(:categoryName,null,:applicationId,1)
registration_insert4 = insert into file_category(category_name,parent_category_id, application_id,visibility) values (?,?,?,1) 
registration_insert5 = insert into hr_bgc(application_id, bgc_done_flag, remark) values (?,?,?)

isSameUsernameOrEmail = select count(*) from pre_emp where username like ? or email like ?

isSameEmail = select count(*) from pre_emp where email like ?

submitData_update = update pre_emp set edit_flag = 0, total_edit_flag = 0 where application_id = ?

approveBgc_update1 = update hr_bgc set bgc_done_flag = 1 where application_id = ?
approveBgc_update2 = update hr_bgc set bgc_done_flag = 2 where application_id = ?

approveRmg_update = update pre_emp set rmg_done_flag = 1 where application_id = ?

approveRmgAdmin_update = update pre_emp set rmg_admin_done_flag = 1 where application_id = ?

rejectRmgAdmin_update = update pre_emp set rmg_admin_done_flag = 0 where application_id = ?

rejectRmg_update = update pre_emp set rmg_done_flag = 0 where application_id = ?

isEmployeeEnabled = select count(*) from pre_emp where application_id = ? and enable_flag = 1

enableDisableEmployee_update1 = update pre_emp set enable_flag = 1 where application_id = ?
enableDisableEmployee_update2 = update pre_emp set enable_flag = 0 where application_id = ?

enableDisableEdit_update1 = update pre_emp set total_edit_flag = 1 where application_id = ?
enableDisableEdit_update2 = update pre_emp set total_edit_flag = 0 where application_id = ?

existingEmployeeRegistration_insert1 = insert into pre_emp(username,password,first_name,last_name, email,contact, address, zip, file_location, pre_flag, edit_flag, enable_flag, rmg_done_flag, rmg_admin_done_flag ) values (:username,:password,:firstName,:lastName,:email,null,null,null,:fileLocation,:preFlag,:editFlag, :enableFlag, :rmgDoneFlag,:rmgAdminDoneFlag)
existingEmployeeRegistration_insert2 = insert into emp values (?,?,?,?,?,?,?,?,?,?)
existingEmployeeRegistration_insert3 = insert into file_category(category_name,parent_category_id, application_id, visibility) values(:categoryName,null,:applicationId,1)
existingEmployeeRegistration_insert4 = insert into file_category(category_name,parent_category_id, application_id,visibility) values (?,?,?,1) 
existingEmployeeRegistration_insert5 = insert into hr_bgc(application_id, bgc_done_flag, remark) values (?,?,?)
